# Generated by DocFX metadata extraction — do not commit.
# These YAML files are regenerated from source code XML doc comments
# every time `docfx metadata` or `docfx build` runs.
*.yml
!toc.yml

# DocFX build output
_site/
obj/
